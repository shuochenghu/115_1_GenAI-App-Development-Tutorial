# 部署到 Streamlit Community Cloud 時，於後台 Settings → Secrets 貼上內容。
# 本機開發不需要這個檔，改用 .env 即可。
# 複製此檔為 .streamlit/secrets.toml 時，記得它已被 .gitignore 排除。
OPENAI_API_KEY = "在這裡貼上你的_OpenAI_API_key"
OPENAI_MODEL = "gpt-5.4-mini"
