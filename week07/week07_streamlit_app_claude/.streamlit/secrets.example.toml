# 這是範例檔。實際的 secrets.toml 已被 .gitignore 忽略，不會上傳。
# 本機測試 secrets：複製本檔為 secrets.toml 並填入 key。
# 雲端部署：改在 Streamlit Community Cloud 的 App → Settings → Secrets 填入相同內容。
OPENAI_API_KEY = "sk-proj-your-key-here"
OPENAI_MODEL = "gpt-5.4-mini"
