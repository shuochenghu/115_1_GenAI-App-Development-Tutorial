第九週課程重點：文件處理與資料前處理。

第一步依副檔名選擇 reader，將 PDF、Word、CSV 或文字檔轉成統一字串。
第二步清理多餘空白，但保留段落、頁碼與欄位關係。
第三步用 chunk_size 與 overlap 切分長文件，並保存來源位置。

上傳與切塊都在本機完成。只有使用者按下 AI 摘要按鈕後才呼叫 API。
下週會把 chunks 轉成 embedding，建立語意搜尋。
